"""Node classification — partition extracted nodes by their role in the project.

Cut 1 ships a pure path/filename ``operational_classifier`` (zero-graph,
deterministic). Future cuts (reachability/coherence, Git-time, behavioural red
flags) join this package as additional classifiers over the same node set.
"""
