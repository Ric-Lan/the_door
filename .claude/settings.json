{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "f=\"$CLAUDE_PROJECT_DIR/.claude/hooks/block_prototype_edits.py\"; if command -v python >/dev/null 2>&1 && [ -f \"$f\" ]; then python \"$f\"; else exit 0; fi"
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "f=\"$CLAUDE_PROJECT_DIR/.claude/hooks/block_the_door_serve.py\"; if command -v python >/dev/null 2>&1 && [ -f \"$f\" ]; then python \"$f\"; else exit 0; fi"
          }
        ]
      },
      {
        "matcher": "mcp__the-door__snapshot_write",
        "hooks": [
          {
            "type": "command",
            "command": "f=\"$CLAUDE_PROJECT_DIR/.claude/hooks/c3_gate_snapshot_write.py\"; if command -v python >/dev/null 2>&1 && [ -f \"$f\" ]; then python \"$f\"; else exit 0; fi"
          }
        ]
      },
      {
        "matcher": "mcp__the-door__snapshot_patch",
        "hooks": [
          {
            "type": "command",
            "command": "f=\"$CLAUDE_PROJECT_DIR/.claude/hooks/c3_gate_snapshot_write.py\"; if command -v python >/dev/null 2>&1 && [ -f \"$f\" ]; then python \"$f\"; else exit 0; fi"
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "f=\"$CLAUDE_PROJECT_DIR/.claude/hooks/c4_block_native_exec.py\"; if command -v python >/dev/null 2>&1 && [ -f \"$f\" ]; then python \"$f\"; else exit 0; fi"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo \"⚠ The Door: 前端唯一正式版 = docs/frontend-local-version-viewer/viewer/（不要動 prototype/）；啟動本地伺服器指令是 the-door ui <test-target>，不是 serve。\""
          }
        ]
      }
    ]
  }
}
